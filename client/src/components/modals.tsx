import { CreateNoteModal } from "./create-note-modal";
import { DeleteNoteModal } from "./delete-note-modal";

export const Modals = () => {
  return (
    <>
      <CreateNoteModal />
      <DeleteNoteModal />
    </>
  );
};
